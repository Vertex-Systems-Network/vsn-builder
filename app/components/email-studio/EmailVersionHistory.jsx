import { Clock3, RotateCcw } from "lucide-react";

function stamp(value){try{return new Intl.DateTimeFormat(undefined,{dateStyle:"medium",timeStyle:"short"}).format(new Date(value));}catch{return String(value||"");}}
export function EmailVersionHistory({versions=[],onRestore}){return <div className="vsn-email-studio-side-scroll"><div className="vsn-email-history-intro"><Clock3 size={16}/><div><strong>Version history</strong><span>VSN stores the previous state before each manual save. Restore locally, review, then save again.</span></div></div><div className="vsn-email-history-list">{versions.map((version,index)=><article key={version.id}><div><strong>{version.label||`Version ${versions.length-index}`}</strong><span>{stamp(version.createdAt)}</span><small>{version.meta?.subject||version.meta?.name||"Email snapshot"} · {version.document?.blocks?.length||0} blocks</small></div><button type="button" onClick={()=>onRestore?.(version)}><RotateCcw size={12}/>Restore</button></article>)}{!versions.length?<div className="vsn-email-studio-empty">No saved versions yet. Version history begins after the first subsequent save.</div>:null}</div></div>}
