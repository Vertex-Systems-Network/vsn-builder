# Release Archive Policy

Release reports that are still referenced by compatibility/package audits remain at the repository root for backward compatibility. Exact duplicate copies under `docs/` are not packaged. New milestone reports should have one canonical copy and be linked from documentation instead of duplicated.
