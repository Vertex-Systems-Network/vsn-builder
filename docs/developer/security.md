# Security

Preserve tenant isolation, server authorization, signed app-proxy trust boundaries, output escaping, safe SVG handling, CSP warnings, destructive confirmations and privacy webhook lifecycle.

## Engineering rules

- Keep client-safe and server-only dependencies separated.
- Reuse shared contracts instead of parallel engines.
- Enforce permissions on the server.
- Add acceptance criteria and a regression audit for meaningful platform changes.
