# Loop Builder Demo

Create a Loop / Query layout, choose a Shopify source (Products, Collections, Blogs, Articles, Search, Metaobjects or an SDK provider), configure filters/sort/limit, then bind child widgets to the current loop item. Preview empty/loading/error states and pagination or Load More before publishing.
