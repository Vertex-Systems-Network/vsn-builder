# AI Editable Output Demo

Enable the AI Builder feature and configure `OPENAI_API_KEY`. Generate a section/page/layout alternative. AI output is constrained to supported VSN schema and existing widgets, then passes responsive/accessibility validation. The result stays editable and supports Apply / Discard plus normal undo; it is not raw generated HTML.
