# Canvas → Shopify Native Section

Build a reusable VSN section, validate Canvas/Preview/Storefront output, expose supported content through the Native Shopify Bridge, then add the app section from Shopify Theme Editor. VSN keeps the shared renderer as the source of truth.
