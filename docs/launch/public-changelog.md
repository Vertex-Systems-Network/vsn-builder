# Public Changelog Workflow

`app/data/releases.js` is the in-app public release source. Each product release should include a concise version, title, date, summary and user-visible highlights. Internal migration/checksum details belong in release reports rather than the public changelog.
