# Performance Standard

Prefer used-assets-only delivery, lazy/conditional runtime loading and bounded DOM work. New editor features must avoid global listeners per widget and repeated full-tree traversal on every render. New dependencies require bundle/runtime justification.
